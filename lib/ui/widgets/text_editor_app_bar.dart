import 'package:flutter/material.dart';

class TextEditorAppBar extends AppBar {
  TextEditorAppBar({super.key});

  Widget build(BuildContext context) {
    return AppBar(
      title: const Text(''),
    );
  }
}
